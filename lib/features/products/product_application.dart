/// بيانات Mock لطلب منتج بنكي (تمويل / ادخار / استثمار / تأمين)
class ProductApplication {
  final String amount;
  final String duration;

  const ProductApplication({required this.amount, required this.duration});
}
